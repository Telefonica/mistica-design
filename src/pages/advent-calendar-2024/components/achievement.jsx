import { IconBugFilled, Box } from "@telefonica/mistica";

const Achievement = ({ achievement }) => {
  return (
    <Box padding={8} style={{ border: "1px solid #ccc" }}>
      <IconBugFilled />
    </Box>
  );
};

export default Achievement;
